def solution(arr1, arr2):
    row = len(arr1)
    col = len(arr1[0])
    
    result = []
    
    for i in range(row):
        temp_row = []
        for j in range(col):
            temp_row.append(arr1[i][j] + arr2[i][j])
        result.append(temp_row)
    
    return result
