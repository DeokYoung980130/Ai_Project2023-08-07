def solution(arr):
    if len(arr) <= 1:
        return [-1]
    
    min_index = arr.index(min(arr))
    
    return arr[:min_index] + arr[min_index + 1:]

arr1 = [4, 3, 2, 1]
print(solution(arr1))

arr2 = [10]
print(solution(arr2))
